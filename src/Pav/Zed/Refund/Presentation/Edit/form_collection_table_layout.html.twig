{% use "form_div_layout.html.twig" %}

{%- block form_widget -%}

    {% if compound %}
        {{- block('form_widget_compound') -}}
    {% else %}
        {{- block('form_widget_simple') -}}
    {% endif %}

{%- endblock form_widget -%}

{%- block form_widget_simple -%}
    {%- set type = type|default('text') -%}
    {%- if disabled -%}
        {% if value is not empty %}{{ value }}{% endif %}
    {%- else  -%}
        <input
                class="form-control"
                type="{{ type }}"
                {{ block('widget_attributes') }}
                {% if value is not empty %}value="{{ value }}" {% endif %}
        />
    {%- endif  -%}
{%- endblock form_widget_simple -%}

{%- block form_row -%}

    <td>
        {{- form_widget(form) -}}
    </td>

{%- endblock form_row -%}

{%- block form -%}

    {{- form_start(form) -}}
    {{- form_errors(form) -}}

        <table class="table table-striped table-bordered table-hover gui-table-data dataTable">

            {% if form.header_row is defined %}
                <thead>
                    <tr class="row">
                        {% for headerColumn in form.header_row|keys %}

                            <th>{{ headerColumn }}</th>
                        {% endfor %}
                    </tr>
                </thead>
            {% endif %}

            {{- form_widget(form.table_body) -}}

        </table>

    {{- form_end(form) -}}

{%- endblock form -%}


{%- block collection_widget -%}

    {% if prototype is defined %}
        {%- set attr = attr|merge({'data-prototype': form_row(prototype) }) -%}
    {% endif %}

    {% for child in form %}
        <tr role="row">
            {{- form_row(child) -}}
        </tr>
    {% endfor %}

{%- endblock collection_widget -%}

{%- block form_rest -%}

{%- endblock form_rest -%}

{%- block money_widget -%}
<div class="input-group">
    {{ '{{ widget }}<span class="input-group-addon"> € </span>'|replace({ '{{ widget }}': block('form_widget_simple') })|raw }}
</div>


{%- endblock money_widget -%}
